export const MAIN_TABS=Object.freeze(['dashboard','hoje','disciplinas','calendario','agenda','questoes','metas','instrucoes']);
export function nextNavigationIndex(current,length,key){if(!length)return-1;if(key==='Home')return 0;if(key==='End')return length-1;const delta=['ArrowRight','ArrowDown'].includes(key)?1:-1;return(current+delta+length)%length}

export function createNavigationController({document,window,render=()=>{},trapModalTab=()=>{},closeReview=()=>{}}={}){
  if(!document||!window)throw new TypeError('Controlador de navegação requer documento e janela.');
  const moreButton=document.getElementById('moreTabButton'),moreMenu=document.getElementById('mobileMoreMenu');
  const syncMore=tabName=>{const secondary=['agenda','questoes','metas','instrucoes'].includes(tabName);moreButton?.classList.toggle('active',secondary);moreMenu?.querySelectorAll('[data-more-tab]').forEach(item=>item.classList.toggle('active',item.dataset.moreTab===tabName))};
  const closeMore=({restoreFocus=false}={})=>{if(!moreMenu)return;moreMenu.hidden=true;moreButton?.setAttribute('aria-expanded','false');if(restoreFocus)moreButton?.focus()};
  const activate=(tabName,updateHash=true)=>{const button=document.querySelector(`.tab-btn[data-tab="${tabName}"]`),panel=document.getElementById(`panel-${tabName}`);if(!button||!panel)return false;document.querySelectorAll('.tab-btn').forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-selected',String(active));item.tabIndex=active?0:-1});document.querySelectorAll('.panel').forEach(item=>item.classList.toggle('active',item===panel));syncMore(tabName);button.scrollIntoView?.({block:'nearest',inline:'nearest',behavior:'smooth'});render(tabName);if(updateHash)window.history.replaceState(null,'',`#${tabName}`);return true};
  moreButton?.addEventListener('click',()=>{const open=moreMenu?.hidden;if(!moreMenu)return;moreMenu.hidden=!open;moreButton.setAttribute('aria-expanded',String(open));if(open)moreMenu.querySelector('[role="menuitem"]')?.focus()});
  moreMenu?.addEventListener('click',event=>{const item=event.target.closest('[data-more-tab]');if(item){activate(item.dataset.moreTab);closeMore()}});
  moreMenu?.addEventListener('keydown',event=>{const items=[...moreMenu.querySelectorAll('[role="menuitem"]')],index=items.indexOf(document.activeElement);if(event.key==='Escape'){event.preventDefault();closeMore({restoreFocus:true});return}if(!['ArrowDown','ArrowUp','Home','End'].includes(event.key))return;event.preventDefault();items[nextNavigationIndex(index,items.length,event.key)]?.focus()});
  document.addEventListener('click',event=>{if(!moreMenu?.hidden&&!moreMenu.contains(event.target)&&event.target!==moreButton)closeMore()});
  document.querySelectorAll('.tab-btn[data-tab]').forEach(button=>{button.addEventListener('click',()=>activate(button.dataset.tab));button.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight','Home','End'].includes(event.key))return;event.preventDefault();const tabs=[...document.querySelectorAll('.tab-btn[data-tab]')].filter(item=>window.getComputedStyle(item).display!=='none'),next=nextNavigationIndex(tabs.indexOf(button),tabs.length,event.key);tabs[next]?.focus();activate(tabs[next]?.dataset.tab)})});
  const registerShortcuts=()=>document.addEventListener('keydown',event=>{trapModalTab(event);const modifier=window.navigator.platform.toUpperCase().includes('MAC')?event.metaKey:event.ctrlKey,active=document.activeElement,typing=['INPUT','TEXTAREA','SELECT'].includes(active?.tagName)||active?.isContentEditable;if(modifier&&event.key.toLowerCase()==='k'){event.preventDefault();document.getElementById('globalSearchInput')?.focus();return}if(event.key==='Escape'){document.getElementById('globalSearchInput')?.blur();document.getElementById('globalSearchResults')?.classList.remove('show');if(document.getElementById('reviewRatingOverlay')?.classList.contains('show')){event.preventDefault();closeReview();return}if(document.getElementById('sessionModalOverlay')?.classList.contains('show')){event.preventDefault();document.getElementById('sessionModalSkipBtn')?.click();return}if(document.getElementById('modalOverlay')?.classList.contains('show')){event.preventDefault();document.getElementById('modalCancelBtn')?.click()}return}if(!typing&&/^[1-8]$/.test(event.key))activate(MAIN_TABS[Number(event.key)-1])});
  return Object.freeze({activate,closeMore,registerShortcuts});
}
