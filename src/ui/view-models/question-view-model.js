export function buildQuestionViewModel(item,{formatDate,getSubjectName,getTopicName,subjectIdOf,accuracy}={}){const resolved=Number(item.resolved)||0,correct=Number(item.correct)||0;return{date:item.date?formatDate(item.date):'Sem data',subject:getSubjectName(subjectIdOf(item))||'Sem disciplina',topic:item.topicId?getTopicName(item.topicId):'Sem tópico',resolved,correct,accuracy:accuracy(correct,resolved),errors:Math.max(0,resolved-correct)}}
