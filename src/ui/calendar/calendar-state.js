export function createCalendarState(initial={}){return{month:initial.month instanceof Date?new Date(initial.month):new Date(),filters:{subjectId:initial.filters?.subjectId||'',status:initial.filters?.status||'',month:initial.filters?.month||'',type:initial.filters?.type||''},view:initial.view||'month',visible:Number(initial.visible)||10,editingId:null,draft:null,isNew:false,selectedId:null,expandedIds:new Set()};}
