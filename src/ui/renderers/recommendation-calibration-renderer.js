export function renderRecommendationCalibrationModel(model,{escapeHtml=value=>String(value)}={}){const rows=model?.groups?.factor||[];if(!rows.length)return'<div class="upcoming-empty">Conclua recomendações e registre resultados para iniciar a calibração.</div>';return`<div class="calibration-grid">${rows.map(item=>`<article><strong>${escapeHtml(item.label)}</strong><span>${item.total} resultados · ${item.positive} positivos</span><b>${item.positiveRate===null?'Amostra insuficiente':item.positiveRate+'% positivos'}</b><small>Confiança ${escapeHtml(item.confidence.toLowerCase())}</small></article>`).join('')}</div><p class="analytics-note">Amostra mínima: ${model.minimumSample} resultados atribuíveis por grupo · algoritmo ${model.algorithmVersion}.</p>`}
