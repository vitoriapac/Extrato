export const normalizeExamImportSearch=value=>String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('pt-BR');
export function buildExamImportViewModel({state,preset,topicLabel=()=>'',preview=null}={}){const query=normalizeExamImportSearch(state?.query?.trim()),subjects=(preset?.subjects||[]).map(subject=>({...subject,topics:(subject.topics||[]).filter(topic=>!query||normalizeExamImportSearch([subject.name,topic.name,...(subject.aliases||[]),...(topic.aliases||[]),topicLabel(topic),...(topic.sourceRefs||[])].join(' ')).includes(query))})).filter(subject=>subject.topics.length);return{step:state.step,preset,subjects,selectedTopics:state.topicIds.size,totalTopics:(preset?.subjects||[]).reduce((sum,subject)=>sum+(subject.topics||[]).length,0),preview}}
