export function toggleExamSubject(state,preset,subjectId,checked){const subject=(preset?.subjects||[]).find(item=>item.id===subjectId);checked?state.subjectIds.add(subjectId):state.subjectIds.delete(subjectId);for(const topic of subject?.topics||[]){const key=`${subjectId}:${topic.id}`;checked?state.topicIds.add(key):state.topicIds.delete(key)}return state}
export function toggleExamTopic(state,key,checked){checked?state.topicIds.add(key):state.topicIds.delete(key);return state}
export function selectExamTopics(state,preset,predicate){state.subjectIds.clear();state.topicIds.clear();for(const subject of preset?.subjects||[])for(const topic of subject.topics||[])if(predicate(topic,subject)){state.subjectIds.add(subject.id);state.topicIds.add(`${subject.id}:${topic.id}`)}return state}
