import {buildStructuredImportViewModel} from './structured-import-view-model.js';
export function createStructuredImportController({document,window,parse,service,render,notify,onImported,maxBytes=2*1024*1024}={}){
  const input=document.getElementById('structuredContentFile'),open=document.getElementById('structuredContentImportBtn'),overlay=document.getElementById('structuredImportOverlay'),content=document.getElementById('structuredImportContent'),confirm=document.getElementById('structuredImportConfirmBtn'),cancel=document.getElementById('structuredImportCancelBtn');let pending=null,previousFocus=null;
  const close=()=>{overlay.classList.remove('show');pending=null;previousFocus?.focus?.();previousFocus=null};
  const show=model=>{content.innerHTML=render(model);confirm.hidden=!model.canConfirm;overlay.classList.add('show');cancel.focus()};
  const read=file=>{if(!file)return;if(file.size>maxBytes){notify('O arquivo excede o limite de 2 MB.');return}const reader=new window.FileReader();reader.onerror=()=>notify('Não foi possível ler o arquivo.');reader.onload=()=>{try{const subjects=parse(String(reader.result||''),{fileName:file.name||''}),preview=service.preview(subjects);pending={subjects,fileName:file.name||'arquivo'};show(buildStructuredImportViewModel({...pending,preview}))}catch(error){pending=null;show(buildStructuredImportViewModel({fileName:file.name||'arquivo',issues:error?.issues||[{message:error?.message||'Arquivo inválido.'}]}))}};reader.readAsText(file)};
  open.addEventListener('click',()=>{previousFocus=document.activeElement;input.click()});input.addEventListener('change',()=>{read(input.files?.[0]);input.value=''});cancel.addEventListener('click',close);confirm.addEventListener('click',()=>{if(!pending)return;const result=service.import(pending.subjects);close();onImported(result)});overlay.addEventListener('keydown',event=>{if(event.key==='Escape'){event.preventDefault();close()}});return Object.freeze({read,close});
}
