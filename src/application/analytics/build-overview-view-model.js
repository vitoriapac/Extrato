const sum=(items,selector)=>items.reduce((total,item)=>total+(Number(selector(item))||0),0);
export function buildStudyTimeViewModel({sessions=[],today,weekStart,monthStart,hoursForDate,addDays}={}){
  const dated=sessions.filter(item=>item.date),totalSeconds=sum(dated,item=>item.durationSeconds),todaySeconds=sum(dated.filter(item=>item.date===today),item=>item.durationSeconds),weekSeconds=sum(dated.filter(item=>item.date>=weekStart&&item.date<=today),item=>item.durationSeconds),monthSeconds=sum(dated.filter(item=>item.date>=monthStart&&item.date<=today),item=>item.durationSeconds);
  const firstRecent=dated.filter(item=>item.date>=addDays(today,-29)&&item.date<=today).map(item=>item.date).sort()[0]||null;
  const days=firstRecent?Math.floor((new Date(today+'T00:00:00')-new Date(firstRecent+'T00:00:00'))/86400000)+1:0,recent=firstRecent?dated.filter(item=>item.date>=firstRecent&&item.date<=today):[],realized=sum(recent,item=>item.durationSeconds);let planned=0;for(let index=0;index<days;index++)planned+=hoursForDate(addDays(firstRecent,index))*3600;
  const elapsed=Math.floor((new Date(today+'T00:00:00')-new Date(weekStart+'T00:00:00'))/86400000)+1,byDate=Object.groupBy?Object.groupBy(dated.filter(item=>item.date>=weekStart&&item.date<=today),item=>item.date):dated.filter(item=>item.date>=weekStart&&item.date<=today).reduce((map,item)=>((map[item.date]??=[]).push(item),map),{});let achieved=0;for(let index=0;index<elapsed;index++){const date=addDays(weekStart,index),target=hoursForDate(date)*3600,actual=sum(byDate[date]||[],item=>item.durationSeconds);if(target>0&&actual>=target)achieved++}
  const targetSeconds=hoursForDate(today)*3600;
  return {todaySeconds,weekSeconds,monthSeconds,totalSeconds,targetSeconds,todayGoalPct:targetSeconds>0?Math.round(todaySeconds/targetSeconds*100):0,consistency:{achieved,elapsed},pace:{secondsPerDay:days?realized/days:0,days},dedication:{score:planned>0?Math.min(100,Math.round(realized/planned*100)):0,realized,planned,days}};
}
