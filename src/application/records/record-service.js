export function createRecordService({repository,clock,idGenerator,prefix,normalize=value=>value}={}){if(!repository||!clock||typeof idGenerator!=='function')throw new TypeError('Serviço de registros requer dependências.');return Object.freeze({list:()=>repository.all(),find:id=>repository.findById(id),create:input=>repository.add(normalize({id:idGenerator(prefix),createdAt:clock.nowISO(),...input})),update:(id,changes)=>{const current=repository.findById(id);return current?repository.update(id,normalize({...current,...changes})):null},remove:id=>repository.remove(id)})}
