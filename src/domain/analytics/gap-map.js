export const GAP_MAP_VERSION='1.0.0';
export function buildGapMap(rows=[],{limit=10}={}){const list=(Array.isArray(rows)?rows:[]).map(row=>{const mastery=Number.isFinite(Number(row.mastery))?Number(row.mastery):null,impact=Number(row.examImpact)||0,retention=Number(row.retention)||0,trendRisk=Number(row.trendRisk)||0,coverage=Number(row.coverage)||0;const gap=mastery==null?null:Math.max(0,100-mastery);const score=Math.round(impact*.4+retention*.25+trendRisk*.2+(100-coverage)*.15);return{...row,gap,priority:score,severity:score>=70?'critical':score>=45?'high':score>=25?'medium':'low',reason:gap==null?'Sem evidência de domínio':`Gap de ${gap} pontos com impacto de prova ${impact}%`,recommendedAction:score>=60?'Revisar e resolver questões':'Manter revisão espaçada'};}).sort((a,b)=>b.priority-a.priority);return{algorithmVersion:GAP_MAP_VERSION,state:list.length?'available':'insufficient',items:list.slice(0,limit),total:list.length};}
