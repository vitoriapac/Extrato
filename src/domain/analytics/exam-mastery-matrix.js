export function buildExamMasteryMatrix({subjects=[],blueprint={},metricsByTopic={},masteryTargets={}}={}){const general=Number(blueprint.masteryTarget??80);return subjects.filter(s=>!s.archived).map(subject=>{const config=(blueprint.subjects||[]).find(x=>x.subjectId===subject.id),target=Number(config?.masteryTarget??masteryTargets[subject.id]??general),topics=(subject.topics||[]).filter(t=>!t.archived).map(topic=>{const m=metricsByTopic[topic.id]||{},mastery=m.mastery?.value??m.mastery?.score??null,coverage=m.coverage??(topic.status==='Concluído'?100:topic.status==='Em andamento'?50:0);return{subjectId:subject.id,topicId:topic.id,name:topic.name,coverage,mastery,retention:m.retention?.value??m.retention?.score??null,trend:m.trend||null,priority:m.priority?.value??m.priority?.score??null,confidence:m.mastery?.confidence??0,target,gap:mastery==null?null:Math.round((target-mastery)*10)/10,state:mastery==null?(coverage?'without_evidence':'not_started'):mastery<target?'fragile':'on_target'}});const known=topics.filter(t=>t.mastery!=null),avg=key=>{const rows=topics.filter(t=>t[key]!=null);return rows.length?Math.round(rows.reduce((n,t)=>n+t[key],0)/rows.length):null};return{subjectId:subject.id,name:subject.name,target,coverage:avg('coverage'),mastery:avg('mastery'),retention:avg('retention'),gap:known.length?Math.round((target-avg('mastery'))*10)/10:null,topics}})}
