export const DECISION_HISTORY_VERSION='1.0.0';
export function buildDecisionHistory(feedback=[],{limit=50}={}){const items=(Array.isArray(feedback)?feedback:[]).map(item=>{const snapshot=item.snapshot||{};return{id:item.id||item.recommendationId||null,recommendationId:item.recommendationId||item.id||null,createdAt:item.createdAt||item.date||null,subjectId:item.subjectId||snapshot.subjectId||null,topicId:item.topicId||snapshot.topicId||null,priorityScore:snapshot.priorityScore??item.score??null,reasons:Array.isArray(snapshot.reasons)?[...snapshot.reasons]:[],strategy:snapshot.strategy||null,algorithmVersion:snapshot.algorithmVersion||item.algorithmVersion||null,outcome:item.outcome?{state:item.outcome.state||'insufficient',delta:item.outcome.delta||null,measuredAt:item.outcome.measuredAt||null}:null};}).sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));return{algorithmVersion:DECISION_HISTORY_VERSION,total:items.length,items:items.slice(0,limit)};}
