export const STUDY_STRATEGY_VERSION='1.0.0';
export function buildStudyStrategy(candidate={},options={}){const total=Math.max(15,Math.round(Number(options.availableMinutes??candidate.estimatedMinutes)||30)),error=candidate.diagnosis?.dominantError?.key||null;let strategyType='theory',label='Teoria orientada',parts=[['study','Teoria',.7],['review','Recapitulação',.3]];if(error){strategyType='error_correction';label='Correção de erros';parts=[['review','Revisar erros',.35],['questions','Prática dirigida',.45],['review','Correção',.2]]}else if((candidate.retentionRisk??0)>=40||(candidate.reviewHealthRisk??0)>=40){strategyType='active_review';label='Revisão ativa';parts=[['review','Recuperação ativa',.45],['questions','Questões',.4],['review','Correção',.15]]}else if((candidate.masteryGap??0)>=40){strategyType='directed_practice';label='Prática dirigida';parts=[['review','Revisão curta',.2],['questions','Questões',.65],['review','Correção',.15]]}else if(candidate.covered){strategyType='maintenance';label='Manutenção';parts=[['questions','Questões',.7],['review','Revisão',.3]]}let used=0;const steps=parts.map((p,i)=>{const minutes=i===parts.length-1?total-used:Math.max(5,Math.round(total*p[2]/5)*5);used+=minutes;return{id:`step-${i+1}`,type:p[0],label:p[1],minutes,status:'pending'}});return{id:`strategy:${candidate.topicId||candidate.id}:${Date.now()}`,algorithmVersion:STUDY_STRATEGY_VERSION,strategyType,label,totalMinutes:steps.reduce((n,s)=>n+s.minutes,0),steps,reasons:[...(candidate.reasons||[])],evidence:candidate.evidence||null}}
