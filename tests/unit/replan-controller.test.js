import test from 'node:test';import assert from 'node:assert/strict';import {createReplanController} from '../../src/application/planning/replan-controller.js';
test('controlador calcula capacidade líquida, confirma e limpa a prévia',()=>{const state={dailyPlans:[{date:'2026-09-15',items:[{plannedMinutes:30}]}]},calls=[],addDays=(date,days)=>{const value=new Date(`${date}T12:00:00Z`);value.setUTCDate(value.getUTCDate()+days);return value.toISOString().slice(0,10)},service={calculate:input=>{calls.push(input);return{state:'proposal'}},confirm:()=>({result:{createdItems:1}}),undo:id=>({id,protectedItems:[]})},controller=createReplanController({service,repository:{getDailyPlans:()=>state.dailyPlans},getState:()=>state,clock:{today:()=> '2026-09-14',startOfWeek:()=> '2026-09-14',addDays},getDailyCapacity:()=>120});controller.calculate();assert.equal(calls[0].futureDays[0].availableMinutes,90);assert.deepEqual(calls[0].plans,[]);assert.deepEqual(controller.confirm(),{result:{createdItems:1}});assert.equal(controller.view(),null);assert.deepEqual(controller.undo('a1'),{id:'a1',protectedItems:[]})});
