import test from 'node:test';import assert from 'node:assert/strict';import {createDemoController} from '../../src/ui/controllers/demo-controller.js';
function node(dataset={}){const handlers={};return{dataset,hidden:false,disabled:false,title:'',addEventListener:(name,handler)=>handlers[name]=handler,removeEventListener:name=>delete handlers[name],click:()=>handlers.click?.(),handlers}}
test('controlador sincroniza proteção e gerencia o ciclo da demo',()=>{const nodes={demoBanner:node(),enterDemoBtn:node(),demoEmptyCta:node(),resetDemoBtn:node(),exitDemoBtn:node(),demoReportShortcut:node(),exportReportBtn:node()},target=node({demoTarget:'hoje'}),storage={values:new Map(),setItem(key,value){this.values.set(key,value)}},calls=[],document={getElementById:id=>nodes[id],querySelectorAll:selector=>selector==='[data-demo-protected]'?[target]:selector==='[data-demo-target]'?[target]:[]},controller=createDemoController({document,storage,demoKey:'demo',flashKey:'flash',isDemo:true,getState:()=>({subjects:[],studySessions:[]}),enterMode:()=>calls.push('enter'),resetMode:()=>calls.push('reset'),exitMode:()=>calls.push('exit'),confirm:(_message,action)=>action(),reload:()=>calls.push('reload'),activateTab:name=>calls.push(name)});controller.mount();assert.equal(nodes.demoBanner.hidden,false);assert.equal(nodes.enterDemoBtn.hidden,true);assert.equal(target.disabled,true);nodes.resetDemoBtn.click();nodes.exitDemoBtn.click();target.click();assert.deepEqual(calls,['reset','reload','exit','reload','hoje']);assert.match(storage.values.get('flash'),/restaurados/)});
