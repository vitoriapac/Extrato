import test from 'node:test';import assert from 'node:assert/strict';import {buildStudyTimeViewModel} from '../../src/application/analytics/build-overview-view-model.js';
const addDays=(date,amount)=>{const [year,month,day]=date.split('-').map(Number),value=new Date(year,month-1,day+amount,12,0,0);return [value.getFullYear(),String(value.getMonth()+1).padStart(2,'0'),String(value.getDate()).padStart(2,'0')].join('-')};
test('compõe métricas de tempo da visão geral sem depender do DOM',()=>{const model=buildStudyTimeViewModel({sessions:[{date:'2026-09-01',durationSeconds:3600},{date:'2026-09-05',durationSeconds:7200}],today:'2026-09-05',weekStart:'2026-08-31',monthStart:'2026-09-01',hoursForDate:()=>2,addDays});assert.equal(model.todaySeconds,7200);assert.equal(model.weekSeconds,10800);assert.equal(model.monthSeconds,10800);assert.equal(model.todayGoalPct,100);assert.equal(model.consistency.achieved,1);assert.equal(model.dedication.realized,10800)});
test('mantém métricas neutras quando ainda não há sessões',()=>{const model=buildStudyTimeViewModel({sessions:[],today:'2026-09-05',weekStart:'2026-08-31',monthStart:'2026-09-01',hoursForDate:()=>0,addDays});assert.equal(model.totalSeconds,0);assert.equal(model.todayGoalPct,0);assert.equal(model.dedication.score,0)});
test('cobre limites do período sem depender de UTC',()=>{const model=buildStudyTimeViewModel({sessions:[{date:'2026-09-01',durationSeconds:1800},{date:'2026-09-05',durationSeconds:3600},{date:'2026-09-06',durationSeconds:7200}],today:'2026-09-05',weekStart:'2026-09-01',monthStart:'2026-09-01',hoursForDate:()=>1,addDays});assert.equal(model.todaySeconds,3600);assert.equal(model.weekSeconds,5400);assert.equal(model.monthSeconds,5400);assert.equal(model.dedication.realized,5400)});
test('soma sessões próximas da meia-noite pela data local persistida',()=>{const model=buildStudyTimeViewModel({sessions:[{date:'2026-09-05',durationSeconds:1800},{date:'2026-09-05',durationSeconds:1800}],today:'2026-09-05',weekStart:'2026-09-05',monthStart:'2026-09-01',hoursForDate:()=>1,addDays});assert.equal(model.todaySeconds,3600);assert.equal(model.weekSeconds,3600)});
