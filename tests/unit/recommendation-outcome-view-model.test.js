import test from 'node:test';import assert from 'node:assert/strict';import {buildRecommendationOutcomeViewModel} from '../../src/application/recommendations/build-recommendation-outcome-view-model.js';
test('apresenta antes, depois e delta sem alterar o snapshot',()=>{const before={mastery:54,retention:51,risk:78},feedback={recommendationId:'r1',completed:true,snapshot:{before},outcome:{state:'positive',before:{...before},after:{mastery:67,retention:61,risk:62},delta:{mastery:13,retention:10,risk:16},confidence:.72,confidenceLabel:'Alta',questionVolume:40,measuredAt:'2026-09-08',algorithmVersion:1}};const model=buildRecommendationOutcomeViewModel([feedback]);assert.equal(model.state,'improved');assert.equal(model.metrics.length,3);assert.deepEqual(model.metrics.find(item=>item.key==='risk'),{key:'risk',label:'Risco',before:78,after:62,delta:16,available:true});assert.equal(before.mastery,54)});
test('preserva estado insuficiente e explica ausência de comparação',()=>{const model=buildRecommendationOutcomeViewModel([{completed:true,outcome:{state:'insufficient',before:{mastery:50},after:{mastery:60},delta:{mastery:10},reasons:['Amostra pequena']}}]);assert.equal(model.state,'insufficient');assert.deepEqual(model.reasons,['Amostra pequena'])});
test('retorna vazio quando nenhuma recomendação foi medida',()=>{assert.deepEqual(buildRecommendationOutcomeViewModel([]),{state:'empty',available:false,metrics:[]})});
