import test from 'node:test';import assert from 'node:assert/strict';import {buildStudyTrack32ViewModel,selectWeeklyPlans} from '../../src/application/analytics/build-studytrack32-view-model.js';import {buildWeeklyClose} from '../../src/domain/analytics/weekly-close.js';import {buildGapMap} from '../../src/domain/analytics/gap-map.js';import {buildDecisionHistory} from '../../src/domain/recommendations/decision-history.js';import {buildPostSimulationReplan} from '../../src/domain/planning/post-simulation-replan.js';
const addDays=(iso,days)=>{const [y,m,d]=iso.split('-').map(Number),date=new Date(y,m-1,d);date.setDate(date.getDate()+days);return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`};
test('seleciona o plano semanal sem itens descartados',()=>{const result=selectWeeklyPlans([{date:'2026-09-08',items:[{status:'completed',plannedMinutes:30},{status:'skipped',plannedMinutes:40}]}],'2026-09-03','2026-09-09');assert.equal(result.plannedMinutes,30);assert.equal(result.plans.length,1)});
test('constrói o ciclo analítico sem DOM e com datas locais',()=>{const model=buildStudyTrack32ViewModel({today:'2026-09-09',sessions:[{date:'2026-09-08',durationSeconds:1800}],questions:[{date:'2026-09-08',resolved:10,correct:7}],dailyPlans:[{date:'2026-09-08',items:[{status:'completed',plannedMinutes:30}]}],weeklyCapacityMinutes:120,algorithmServices:{addDays,buildWeeklyClose,buildGapMap,buildDecisionHistory,buildPostSimulationReplan,buildCandidates:()=>[]},nameResolvers:{subject:()=>null,topic:()=>null}});assert.equal(model.period.start,'2026-09-03');assert.equal(model.weeklyClose.investment.plannedMinutes,30);assert.equal(model.weeklyClose.execution.rate,100)});
