import test from 'node:test';import assert from 'node:assert/strict';import {createErrorBoundaryController} from '../../src/ui/controllers/error-boundary-controller.js';
test('expõe falha recuperável e permite tentar novamente',()=>{const nodes={appErrorState:{hidden:true},appErrorMessage:{textContent:''},appErrorRetryBtn:{addEventListener:(_name,handler)=>nodes.retry=handler}},document={getElementById:id=>nodes[id]};let retries=0;const controller=createErrorBoundaryController({document,onRetry:()=>retries++});const original=console.error;console.error=()=>{};controller.report(new Error('falha'),'painel semanal');console.error=original;assert.equal(nodes.appErrorState.hidden,false);assert.match(nodes.appErrorMessage.textContent,/painel semanal/);nodes.retry();assert.equal(nodes.appErrorState.hidden,true);assert.equal(retries,1)});
