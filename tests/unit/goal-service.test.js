import test from 'node:test';import assert from 'node:assert/strict';import {createGoalService} from '../../src/application/goals/goal-service.js';
function fixture(){const metas={horasDiarias:2,horasPorDia:{0:1,1:2,2:2,3:2,4:2,5:2,6:1},metaAprovacao:80};const repository={getGoals:()=>metas,updateGoal:(key,value)=>(metas[key]=value,metas),updateDailyHours:(day,value)=>(metas.horasPorDia[String(day)]=value,metas)};return{metas,service:createGoalService({repository,getDayOfWeek:date=>new Date(date+'T00:00:00').getDay()})}}
test('centraliza metas diárias e preserva compatibilidade com a meta legada',()=>{const{metas,service}=fixture();assert.equal(service.hoursForDate('2026-09-07'),2);service.updateDailyHours(1,3.5,{isToday:true});assert.equal(metas.horasPorDia['1'],3.5);assert.equal(metas.horasDiarias,3.5)});
test('limita meta de aprovação e limpa somente o fim de semana',()=>{const{metas,service}=fixture();service.update('metaAprovacao',130);service.clearWeekend();assert.equal(metas.metaAprovacao,100);assert.equal(metas.horasPorDia['0'],0);assert.equal(metas.horasPorDia['6'],0);assert.equal(metas.horasPorDia['1'],2)});
