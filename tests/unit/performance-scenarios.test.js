import test from 'node:test';import assert from 'node:assert/strict';import {buildPerformanceScenarios} from '../../src/domain/forecasts/performance-scenarios.js';
test('não inventa cenários sem projeção disponível',()=>{const result=buildPerformanceScenarios({forecast30:{available:false,reason:'Poucos dados'}});assert.equal(result.available,false);assert.equal(result.scenarios.length,0)});
test('gera cenários versionados e conservadores',()=>{const result=buildPerformanceScenarios({forecast30:{available:true,low:70,high:78,central:74,confidence:.6,confidenceLabel:'Média',slopePerWeek:2},evidence:{sampleSize:200}},{weeklyMinutes:600});assert.deepEqual(result.scenarios.map(x=>x.id),['current','plus_2h','plus_4h']);assert.ok(result.scenarios[2].high>=result.scenarios[0].high);assert.match(result.scenarios[1].premises[1],/Simulação/) });
