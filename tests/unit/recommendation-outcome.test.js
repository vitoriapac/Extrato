import test from 'node:test';import assert from 'node:assert/strict';
import {captureRecommendationBaseline,measureRecommendationOutcome,recommendationOutcomeConfidence} from '../../src/application/recommendations/outcome-service.js';

test('registra uma linha de base normalizada',()=>{assert.deepEqual(captureRecommendationBaseline({accuracy:58,questionVolume:32,retentionScore:44,daysSinceContact:7,measuredAt:'2026-09-01T10:00:00Z'}),{accuracy:58,questionVolume:32,retentionScore:44,daysSinceContact:7,measuredAt:'2026-09-01T10:00:00Z'})});
test('classifica a confiança pelo volume posterior',()=>{assert.equal(recommendationOutcomeConfidence(0),'Aguardando');assert.equal(recommendationOutcomeConfidence(12),'Amostra inicial');assert.equal(recommendationOutcomeConfidence(32),'Estimativa');assert.equal(recommendationOutcomeConfidence(50),'Mais confiável')});
test('não atribui impacto com amostra pequena ou muitas intervenções',()=>{const feedback={};const outcome=measureRecommendationOutcome(feedback,{accuracyAfter:71,questionVolumeAfter:12,measuredAt:'2026-09-02T10:00:00Z',daysElapsed:2,otherActivities:4});assert.equal(outcome.attributionEligible,false);assert.equal(outcome.reasons.length,2);assert.equal(feedback.outcome,outcome)});
test('aceita avaliação com amostra e janela suficientes sem alterar pesos',()=>{const feedback={algorithmVersion:3};const outcome=measureRecommendationOutcome(feedback,{accuracyAfter:71,questionVolumeAfter:52,nextReviewRating:'Bom',retentionAfter:68,measuredAt:'2026-09-08T10:00:00Z',daysElapsed:7,otherActivities:1});assert.equal(outcome.confidence,'Mais confiável');assert.equal(outcome.attributionEligible,true);assert.equal(feedback.algorithmVersion,3)});
