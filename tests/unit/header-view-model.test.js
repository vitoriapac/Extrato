import test from 'node:test';import assert from 'node:assert/strict';import {buildHeaderViewModel} from '../../src/application/analytics/build-header-view-model.js';
test('separa prontidão composta de conteúdo concluído',()=>{const topics=Array.from({length:47},(_,index)=>({id:String(index),status:index<12?'Concluído':'Não iniciado'})),model=buildHeaderViewModel({readiness:{value:42,confidenceLabel:'Alta',availableFactors:['a','b','c','d','e'],missingFactors:[]},subjects:[{id:'s'}],topics});assert.equal(model.readinessValue,42);assert.equal(model.contentPercent,26);assert.equal(model.completed,12);assert.equal(model.availableFactors,5)});
