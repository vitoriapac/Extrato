import {test,expect} from '@playwright/test';import {activateTab} from './helpers.js';
test('aplica CSP e mantém os fluxos básicos',async({page})=>{const errors=[];page.on('console',message=>{if(/content security policy/i.test(message.text()))errors.push(message.text())});await page.goto('/');const policy=await page.locator('meta[http-equiv="Content-Security-Policy"]').getAttribute('content');expect(policy).toContain("script-src 'self'");expect(policy).toContain("object-src 'none'");await activateTab(page,'disciplinas');await page.getByRole('button',{name:/Carregar disciplinas do edital/i}).click();await expect(page.getByRole('heading',{name:'Carregar edital'})).toBeVisible();expect(errors).toEqual([])});
